name: davinci
description: A package to convert any widget to an image which can be saved locally or can be shared to other apps and chats.
version: 2.1.1
homepage: https://github.com/Imgkl/davinci

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
#  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter: